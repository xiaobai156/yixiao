# 杀肖-V2 项目专属规则

## 1. 继承、授权与工作方式

本文件仅适用于本项目，继承父目录 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`。其中的授权、来源隔离、准确性、缓存事务和安全边界继续有效；本文件补充项目契约，不把历史报告或备份当作当前规则。`AGENTS - 副本.md` 不得修改。

- 回复使用中文，先称呼“老大”；遵循 `i-have-adhd` 的明确偏好：先结论、必要时编号说明、明确下一步。没有待执行事项时直接说明已完成，不强加确认步骤。
- 代码实现遵循 `ponytail full`：简单、复用、不过度设计；先读受影响代码和实际调用链，不以少代码代替准确性。其实现风格不覆盖 `i-have-adhd` 的表达要求，也不能缩减用户请求的工作。
- Skill 按当前任务适用条件使用，不把其中的流程、输出长度或测试建议扩大为所有任务的硬门禁。Skill 不可用时说明一次，并继续执行本文件中已明确的偏好；不得声称已加载。必要测试、安全检查和未完成事项不能因简短而省略。
- 解释、审计或诊断不自动授权修复。实施任务取得授权且范围明确后，持续完成范围内的修改、适度验证和结果汇报；已满足的“开始”或正式授权不重复索取。
- 仅在缺少会实质影响结果的信息、下一步超出授权范围，或尚未取得上级要求的正式切换授权时询问。一个站点受阻只暂停该站点，继续处理同批其他已授权且相互独立的站点。
- 默认在当前任务内完成工作，不强制使用多模型或子 agent。用户明确指定外部模型、路由、数量或角色时保留该要求；不可用时说明，不擅自换模型或外发数据。
- 工作范围只限“杀肖-V2”；不改其他子项目、不建跨项目公共库、不做跨项目统一架构。其他项目的配置、缓存和候选不得补入本项目。
- 对部署、发布、消息发送、凭据处理、重要数据删除、封存、恢复及正式数据覆盖的授权边界不因上述自主执行原则而放宽。保持用户已有未提交改动，不顺带回退、清理或重建。

## 2. 项目入口与路径

- 生产项目：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀肖-V2`。
- 源码：`src\zodiac_v2`；正式配置：`sites.json`；正式缓存：`recent_10_cache.json`；封存档案：`archived_sites.json`。
- Git 同步：当前项目的 `origin` 已绑定到 `https://github.com/xiaobai156/yixiao.git`。以后每次对本项目完成明确授权的修改并通过必要验证后，自动将本次任务产生的改动提交并推送到该 `origin`；只提交本次任务明确改动的文件，不带入用户已有未提交改动、凭据、临时文件或其他项目。只读审计/验证不产生提交；若无法安全区分本次改动，先暂停推送并报告。
- BAT 进入自身目录，以该目录为项目根，设置 `PYTHONPATH=%CD%\src`。PowerShell 手工执行时使用 `$env:PYTHONPATH = "$PWD\src"`。
- 单期入口：`爬虫-每天杀肖.bat`，执行 `python -X utf8 -m zodiac_v2.cli single --period N --workers 8 --formal`。它默认加载完整 `sites.json`，不适用于限定新增或修复站点的验收。
- 判重入口：`爬虫-每天杀肖 - 检查重复.bat`，执行 `python -X utf8 -m zodiac_v2.cli duplicate --period N --periods 10`；这是缓存判重，不是真实抓取。
- 失败复抓使用 CLI：`python -X utf8 -m zodiac_v2.cli retry --period N --retry-errors 失败文件路径`。历史名称 `复抓失败.bat` 当前不存在，不得假报已运行该 BAT，也不因本条自动重建它。
- `onboard` 提供新增站点只读验收；`repair` 提供失败站八阶段只读验证。这是具体入口的职责，不是所有文档、解释、封存或局部维护都必须走的流程。
- 默认成功目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`。
- 默认失败目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`。
- 新增、修复只抓用户指定站点；不得为正式入口验收、成功率门禁或测试方便运行全站。调用前确认目标集合，优先复用现有定点服务和合并接口，不用全量输出接口覆盖已有 TXT。

## 3. 数据与配置契约

- 每条成功结果是指定期数对应的单个生肖，合法值为 `牛马羊鸡狗猪鼠虎兔龙蛇猴`；零个、多个、非法字符或无法证明唯一性时失败。
- 期数是 `1` 至 `9999` 的整数，拒绝布尔值和空值。执行期由用户指定，不自动猜当期；成功候选必须等于本次目标期，不能用相邻期或旧缓存替代。
- `Candidate` 保留目标期、生肖、原始数据行、来源文档 ID、页面顺序和非空证据；其他证据字段继承父目录规则。
- `SiteConfig` 包含非空 `name`、合法 HTTP/HTTPS `url`、方向、分类、已注册 `parser_id` 和 `source_policy`。`pick` 与 `direction` 不得同时配置；方向只接受 `top`、`顶部`、`上`、`bottom`、`尾部`、`底部`、`下`。
- 正式分类为 `已有站点` 和 `新增的站点`；加载旧配置时的缺省及别名由 `config.py` 处理，不能以兼容缺省替代新站分类选择。
- 取源策略由 `cli.py` 的 `SOURCE_POLICIES` 注册集合和站点配置共同限定。当前实现支持 `http_documents`、`http_consensus`、`http_named_topic`、`http_period_keyword_article`、`api_then_http`、`browser`、`browser_user`、`http_then_browser`。实现支持不等于授权某站切换策略；新增或切换仍须目标站真实验证和正式授权。
- 配置站名不得重复；项目配置/缓存身份含站名、规范化 URL、方向和分类。改名或分类不意味着获得重复添加同一来源、栏目和字段的许可。

## 4. 方向与专属解析

- 收集同一高可信目标区块全部有效候选并保留页面顺序，不按整页 HTML 的上下半页切分。
- 本项目采用首尾单组边界：`top` 只接受第一条有效候选，`bottom` 只接受最后一条有效候选。只有一条时同时满足两方向；不使用“靠前/靠后5组”“超过30组”等规则。
- 目标期在区块中但不在对应边界仍失败；目标期缺失、候选为空、真实顺序无法证明或目标期不同合法结果冲突也失败。方向不能消除真实冲突。
- 内部 `left` 枚举只用于已有内部流程，项目配置和正式站点不得使用它绕开方向。
- 每站绑定已注册的专属解析器，未知 `parser_id` 拒绝加载，专属失败不得回落通用解析。
- `family.strict_article` 绑定目标标题、作者或栏目锚点，在同一文章块提取；`family.anchored_section` 在同一页面区块绑定锚点、边界和候选。
- `regex.*` 仅指已登记的站点级规则；`special.*` 用于用户论坛、表格、固定标题/作者、澳门专题、三怪、天空等明确结构，保持对应站点绑定。一次正则命中不能泛化为其他站点授权。
- 期数、杀/禁/绝杀语义和生肖须来自同一记录、表格行或明确数据块。网站地址不能作为标题、作者或栏目语义锚点；URL/topic/文章 ID 仍须用于来源身份核对。
- 其他栏目、作者、开奖、历史块、广告、导航、相邻期和相似格式不得补数。API、主 HTML、script、iframe、OCR 和渲染结果保持独立来源，不互借锚点或值；任何权威来源的真实冲突按上级规则失败。

## 5. 动态来源与截图

- 已确认 HTTP 能完整、稳定、准确取得目标原始数据时固定 HTTP；确需 JS 时才使用配置允许的浏览器策略。判断不只看 HTTP 200，不能为了“自适应”增加多级 fallback、复杂重试、代理池、熔断或调度。
- 浏览器只取得动态正文，不裁决期数、方向、字段或冲突；使用时尽量复用既有浏览器/Context。规则整理不自动授权重构取源层。
- 对 `/article/admin/`、`/article/manager/` 等动态文章优先请求配置中的 `api_url` 或专属接口；`api_then_http` 仅 API 返回 HTTP 404 时改用页面 URL。
- 页面 HTTP 200 但确为空壳，或目标文章正文为空，才可按配置使用浏览器渲染；正文存在但缺目标期、栏目或生肖时失败，不兜底。
- 网络、TLS、HTTP 500、非可信 JSON、文章 ID 不一致或来源冲突不能通过浏览器兜底。各原始文档须证明属于目标文章 ID、用户、栏目或主题，无法唯一证明时失败。
- `special.user_forum_post` 使用配置的用户 API，按用户身份、主题、文章记录和正文共同锁定目标帖子；不使用其他用户或无关历史帖子补值。
- `http_consensus` 仅对明确配置的版本不稳定站点使用：独立取源5次，指定期至少2次同值；每次检查下一期，任一次发现下一期有效数据即方向失败；指定期/下一期冲突或确认不足均失败。这不是其他站点的通用重试要求。
- 截图作为定位和解释线索，不单独授权抓取或修改。已授权新增/修复时，核对截图的期号格式、目标字段、方向位置、锚点及区块，再以真实页面/API 验证；现有解析正确时复用，不因收到截图必建新解析器。
- 截图不能替代来源证据。截图与真实来源不一致、字段不明或期数和生肖无法同块证明时，该候选不能判成功。用户明确要求人工录入时须单独标识为人工数据，不能冒充真实验证。

## 6. 单期与定点正式写入

- 实时判定不读取正式缓存补值或裁决；定稿后先写授权范围内的成功/失败 TXT，再独立尝试缓存持久化。不是整轮成功后才生成失败 TXT。
- 正式单期流程使用与目标期相符的 `WritePermit.formal_single`；CLI 的 `single --formal` 是批次入口，定点服务也须同等写入许可和统一校验回执。许可对象本身不代替用户授权。
- `single` 非正式模式、`retry`、`repair`、`onboard`、判重、审计、影子和独立验证不写正式配置、缓存或 TXT。验证产物只在隔离位置，哈希检查只覆盖本次可能受影响的正式文件；纯文档任务不因此读取缓存。
- `commit_formal_single` 批次缓存门禁仍为成功数除以本轮实际目标站点数严格大于85%；等于或低于85%时缓存原字节不变，但实时成功/失败 TXT 仍生成。不得为提高成功率多抓站点或改分母。
- 门禁通过后，失败站点写当前失败原因并移除该期旧成功值。门禁未通过或缓存写入失败时，明确报告缓存未更新，不能把旧缓存称作本轮成功，也不能改判实时结果。
- 新增/修复的定点更新只处理用户指定站点的当前期记录；其他站点和其他期缓存保持不变。新增历史建立、历史回填、改身份或封存迁移须分别有对应授权，不能借本期修复顺带执行。
- 缓存旧值与实时新值不同是缓存更新问题，不是实时抓取失败；不得擅自覆盖冲突值或污染失败 TXT，应报告具体未写入项并按用户授权处理。
- 85%批次门禁与“定点失败也更新缓存”无法同时满足时保留门禁，不猜测例外；只暂停受影响的缓存写入，说明需决定的范围，不阻断已授权的独立工作。

## 7. 新增与正式判重

- 默认新站分类为 `新增的站点`；用户已指定 `已有站点` 时按其分类，无须重复确认。已有站点不因再次执行新增流程被移动分类。
- 先检查原始站名及去首尾空格后的重名，再查 URL、topic/article ID、栏目、字段、方向和身份冲突。重名/身份冲突只停止该站后续抓取和入库，其他独立站点继续。
- 新站真实访问页面或公开 API，不能只凭截图、缓存、备份或通用解析通过。默认命中有效正式缓存最新期或上一期；用户明确指定其他基准或特例时只在其范围内执行。页面下一期可追加验证，但不能自行替代基准期。
- 真实历史、专属解析、方向与目标期、正式判重、站点级测试均通过，且获得正式添加授权后，才写配置并执行目标站正式单期输出/缓存验收。正式写入前的输出检查使用隔离位置，不形成“先写正式 TXT 才准入”的循环。
- `onboard` 通过只表示只读验收通过，不表示已加入。授权只涵盖验证时等待正式授权；已明确取得的添加授权不因步骤切换重复索取。修复切换另按第10节。
- 正式判重使用项目正式检测器、与当前配置一致的有效近10期缓存及候选站真实结果。无共同期、抓取失败或空数据是未完成，不能称“不重复”。
- 按相同具体期号对齐，只比较双方共同期的完整生肖序列，不跨缺失/失败期拼连续；`--periods 10` 是收集窗口，不是共同期号最低门槛。
- 连续1至2期相同不判重，3至5期疑似重复暂停该站准入，6期及以上禁止加入。可在已授权诊断范围内说明具体匹配，不以“疑似重复”终止整批任务。检测器不支持门槛时报告判重未完成；其修复须在授权范围内，不临时替换算法绕过。
- 默认要求10期有效历史；不足10期或3至5期疑似重复只在用户点名授权范围内特例放行，不放宽身份、方向、栏目、字段、数量、来源、边界或同期冲突。
- 保留明确站点特例：`刘伯温咯` 可按用户授权使用9期历史；`六合爆庄` 若已配置为已有站点，不得因新增流程改分类。不得把这些例外扩展给其他站。

## 8. 缓存契约

- 每站最多10条有效记录，按期数倒序、无重复期数、生肖为合法单字符；`fingerprint` 等于记录生肖的原始排序串。
- 身份至少绑定站名、规范化 URL、方向和分类，且满足上级栏目、字段及配置指纹要求。同 URL 不同目录不能互相继承或混用缓存；正式检测器仍按合法来源身份核对重复。
- 保留来源 ID、来源 URL 和证据 SHA-256；证据哈希为64位十六进制值。缓存读取、目录隔离、合并冲突和写入失败不能反向改变本轮任何站点的实时判定。
- 业务判重读取用于 `duplicate` 和 `onboard`；单期持久化阶段可以读取旧缓存保留历史，不得提前读取为实时判定服务。用户授权的缓存审计或人工更新不视为实时取源，也不能冒充抓取验证。
- 配置/缓存绑定失效时不得正式判重；重建只在明确授权范围内进行，不因某站修复自动重建整份缓存。

## 9. TXT 与排行榜

- 已有站点写 `N期-肖.txt`、`N期-肖-失败.txt`；新增站点写 `N期-肖-新增.txt`、`N期-肖-失败-新增.txt`，分别位于成功/失败目录。
- 定点成功写入复用 `output.py` 的 `merge_formal_single_result_output`：新增行放在站点明细尾部、排行榜上方；保留原成功行的内容、值和相对顺序，不重排整份明细。
- 同站同值避免重复追加；已有成功值不同则报告冲突，不擅自覆盖。只有用户明确授权改该值时，才能处理对应站点和期数。
- 失败文件仅增改指定站点当前期失败；正式修复成功后清除其对应失败记录，其他失败保留。不清空整份失败文件，也不改其他分类/期数文件。
- 排行榜三列为“内容、次数、排名”，按次数降序，同次数并列且下一名连续递增，例如 `1、2、2、3`；同次数的生肖次序沿用输出实现。追加成功后按实际成功明细重新统计。
- 不再自动追加“华林”两个字；现有分类分隔及附属内容沿用输出契约，不因修复任意清理历史内容。
- 全批次输出和定点合并是不同操作：全批次模式生成四类 TXT，无内容写“无”；新增/修复只改目标分类对应文件，不为了“四类齐全”覆盖无关文件。
- 网络、来源身份、锚点、字段、期数缺失、方向越界、专属解析、区块/来源/同期冲突需如实区分；解析错误不伪装成网络失败。缓存提示不进入站点失败分类。

## 10. 修复、封存与完成标准

- 站点修复分独立验证和正式切换：取得“开始验证”授权后真实复现目标站、目标期，在隔离策略验证；按父目录要求再次获得“开始正式更新”授权后迁移最小改动。不得把这套站点切换流程套到解释、文档整理或明确授权的封存操作。
- 正式验收仅跑用户指定站点的完整单期抓取、专属解析、统一校验、输出及缓存链路。无需为“BAT验收”运行默认全站 BAT；只通过模拟文本、正则或函数不能宣称站点修复完成。
- 纯文档修改校验规则继承、内部引用、命令和差异即可，不抓站、不读业务缓存、不运行 Python 全套测试。封存等配置/数据操作核对目标迁移和无关记录不变，不以真实抓取作为完成条件。
- 局部解析器/输出修改运行受影响测试和适用的 Python 编译/静态检查；共享流程、统一校验、缓存或存储逻辑、入口代码修改再跑项目完整测试。修改一条缓存数据不等于修改缓存逻辑。
- 新增/修复按受影响行为覆盖正确期、相邻期、不存在期、错栏目/字段、方向、区块/来源/同期冲突、缓存冲突及单期/多期写入边界；复用已有测试，不要求每站重复建设全部框架。完整测试不等于全站联网，使用隔离或模拟环境，不写正式数据。
- 封存须点名授权；将目标配置和完整缓存证据迁入 `archived_sites.json`，同步从活跃配置/缓存移除。封存后不抓取、不写成功、不更新缓存；恢复/删除另须明确授权，不顺带清理历史 TXT 或恢复其他站点。
- 报告分别列出已验证、已正式写入、失败或未完成的项目及原因；站点真实验证或正式流程不足时只报部分完成，不能因此把已经完成的文档或封存任务误报为待抓取。


## 2026-09-09 正式复抓入口修订

本节细化此前“retry只读”的描述：未加 `--formal` 的 retry 与 repair 仍完全只读；用户显式 `retry --formal` 只授权失败清单唯一匹配的站点/指定期，使用 `commit_targeted_formal_single` 定点合并，禁止调用全量输出接口。定点失败保留已有成功行与缓存，只更新失败诊断；定点成功不套用全批次85%门禁。`single --formal` 仍遵守完整目标集合与严格大于85%的批次缓存门禁。所有成功必须由统一验证器正式签发，禁止把只读对象改为可写。

多进程正式写入必须从读取旧文件到最终写入持有 `storage.locked_paths`；发现未完成TXT事务应拒绝继续覆盖并显式恢复。配置、缓存和正式TXT不得作为自动化测试临时数据。历史238/240/241期生产快照测试保留原断言，单独以 `--historical-snapshots` 启用，不为使历史断言通过而回滚当前数据。
