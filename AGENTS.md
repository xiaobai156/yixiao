# 杀肖-V2项目专属规则

本文件仅补充“杀肖-V2”相对上级 `..\AGENTS.md` 的项目差异。上级全部规则继续有效，本文件不得放宽上级的准确性、授权、隔离、缓存、输出、判重和验收要求。

## 1. 项目入口与路径

- 生产项目路径：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀肖-V2`。
- Python源码位于 `src\zodiac_v2`，正式配置为根目录 `sites.json`，正式缓存为 `recent_10_cache.json`。
- 所有BAT进入自身目录后，以该目录为项目根，并设置 `PYTHONPATH=%CD%\src`。
- 单期入口：`爬虫-每天杀肖.bat`，执行 `zodiac_v2.cli single --period N --workers 8 --formal`。
- 判重入口：`爬虫-每天杀肖 - 检查重复.bat`，执行 `zodiac_v2.cli duplicate --period N --periods 10`。
- 失败复抓入口：`复抓失败.bat`，执行 `zodiac_v2.cli retry --period N --retry-errors 失败文件路径`。
- CLI还提供 `onboard` 新增站点只读验收和 `repair` 失败站八阶段只读验证命令。
- 默认成功输出目录：`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳`。
- 默认失败输出目录：`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳失败`。
- 配置、缓存、解析器和输出均不得跨项目读取或写回原项目。

## 2. 数据契约与合法值

- 每条成功结果必须是一个指定期数对应的单个生肖。
- 合法生肖仅限：`牛马羊鸡狗猪鼠虎兔龙蛇猴`。
- 生肖必须是单个字符；零个、多个、非法字符或无法证明唯一性的结果均失败。
- 期数必须是 `1` 至 `9999` 的整数，禁止布尔值、空值、猜测期数和相邻期替代。
- `Candidate` 必须同时包含目标期数、生肖、原始数据行、来源文档ID、页面顺序和非空证据。
- 成功候选的期数必须等于本次手动指定期数。
- 来源文档类型可为HTML、JSON、浏览器渲染、脚本或iframe，但只有配置授权的来源策略可以参与解析。
- `SiteConfig` 必须包含非空 `name`、合法HTTP/HTTPS `url`、`top`/`bottom` 方向、站点分类、已注册 `parser_id` 和已注册 `source_policy`。
- `pick` 与 `direction` 不得同时出现；配置只接受 `top`、`顶部`、`上`、`bottom`、`尾部`、`底部`、`下`。
- 合法取源策略仅限 `http_documents`、`http_consensus`、`http_named_topic`、`api_then_http`、`browser_user`、`http_then_browser`。
- `http_consensus` 仅用于配置明确标记的取源版本不稳定站点：独立取源5次，指定期至少2次得到相同结果才可成功；每次同时检查下一期，任一次发现下一期有效数据即按方向失败；指定期或下一期结果冲突、确认不足均失败。
- `section` 只允许已有站点或 `新增的站点`；缺省按已有站点处理。
- 配置中的站名不得重复；完整站点身份由站名、URL、方向和分类共同确定。

## 3. 方向边界

- 先提取同一高可信目标区块内全部有效候选，并保留页面真实顺序；不得按整页HTML的上下半页切分。
- `top`/`顶部`/`上`：指定期必须是目标区块第一条有效候选。
- `bottom`/`尾部`/`底部`/`下`：指定期必须是目标区块最后一条有效候选。
- 指定期不在对应方向边界必须失败，即使目标区块其他位置存在该期。
- 目标区块只有一条有效候选时，该候选同时满足 `top` 和 `bottom`。
- 同一目标区块内指定期出现多个不同合法生肖，必须报告候选冲突并失败。
- 候选为空、目标期缺失、方向边界不符或页面真实顺序无法证明，均不得成功。
- 本项目不按候选数量设置方向窗口，不使用“超过30组”或“靠前/靠后5组”等数量规则。
- 源码内部存在 `left` 方向枚举，但项目配置和正式站点不得使用该方向。

## 4. 专属解析与来源隔离

- 每个配置站点必须绑定已注册的专属解析器；解析器由注册表统一加载，未知 `parser_id` 直接拒绝配置。
- 专属解析器只提取目标站点、目标栏目、目标文章或目标区块中的候选；正式专属解析失败时不得回退通用解析器。
- `family.strict_article` 必须绑定目标文章标题、作者或栏目锚点，并在同一文章块中提取期数和杀肖字段。
- `family.anchored_section` 必须在同一页面区块内完成锚点、边界和候选绑定，不得跨文档拼接。
- `regex.*` 仅代表已登记的站点级正则解析器；不得把某个正则成功泛化为其他站点的通用授权。
- `special.*` 用于用户论坛、表格、固定标题、固定作者、澳门专题、三怪区块、天空数据区块等明确结构，必须保持对应站点绑定。
- 解析器必须保留原始行、来源ID、页面顺序和证据，不能只返回生肖。
- 期号、杀/禁/绝杀等语义和生肖必须来自同一条记录、同一表格行或同一明确数据块。
- 其他栏目、标题、作者、开奖结果、历史块、广告、导航、相邻期和相似格式不得作为补数来源。
- 不同来源文档之间不得互借锚点、期数或生肖；API、主HTML、脚本、iframe、OCR和浏览器渲染结果必须分别保留来源身份。
- 同一最高优先级来源中出现不同合法结果，必须失败并报告冲突。

## 5. 动态文章、API与浏览器

- 对 `/article/admin/`、`/article/manager/` 等动态文章URL，优先使用配置中的 `api_url` 或专属接口。
- `api_then_http` 先请求专属API；仅API返回HTTP 404时允许改用页面URL。
- 页面HTTP 200但确认为空壳，或目标文章记录的正文为空时，才允许按配置使用浏览器渲染；正文已存在但缺少目标期、目标栏目或生肖时必须失败，不得兜底。
- 网络错误、TLS错误、HTTP 500、非可信JSON、文章ID不一致或来源冲突不得通过浏览器兜底。
- 浏览器只负责取得动态正文，不能改变期数、方向、栏目、字段、数量或冲突校验。
- API、页面、脚本和渲染结果必须证明属于同一文章ID、用户、栏目或主题；无法唯一证明时失败。
- `special.user_forum_post` 必须使用配置的用户API，按用户身份、主题、文章记录和正文共同锁定目标帖子，不能用其他用户或历史帖子补值。

## 6. 截图定制规则

用户提供截图时，必须据此建立站点专属解析结构，并记录：

- 站名、目录名、URL或topic/article ID。
- 标题、作者、栏目或表头锚点。
- 期号格式和目标字段。
- 生肖所在位置。
- 目标区块起止边界。
- 广告、导航和历史内容截断点。
- `top`/`bottom` 方向位置。

截图不能代替真实页面或API取证。截图结构与真实来源不一致、目标字段不明、期号和生肖无法同块证明时必须失败。截图只影响专属解析，不得放宽统一校验。

## 7. 单期与写入

- 所有期数必须由用户手动指定；不得自动猜当期。
- 单期只允许正式单期模式获得写入许可；成功后才可更新缓存和正式TXT。
- 正式单期缓存闸门按本轮总站点数计算：成功站点数 ÷ 总站点数必须严格大于85%才允许更新缓存；等于或低于85%时缓存文件保持原字节不变，但正式成功/失败TXT仍按实时结果生成。
- 缓存闸门通过后，失败站点必须在缓存JSON对应目录写入当前失败原因；若该期已有旧记录，必须移除该期旧值，不能继续暴露为成功。
- 单期非正式运行、`retry`、`repair`、`onboard` 和其他只读模式不得写入正式配置、缓存或TXT。
- 正式单期写入期数必须与输出文件期数和写入许可完全一致。

## 8. 新增站点

- 新站点默认分类为 `section: "新增的站点"`，不得自动放入已有站点分类。
- 新增前必须先检查原始站名和去除首尾空格后的站名重名，再检查URL、topic/article ID、栏目、字段、方向和身份冲突。
- 重名、身份冲突或疑似重复时立即停止，不得继续抓取、判重、写缓存或写TXT。
- 新站必须真实访问页面或公开API，不能仅凭截图、缓存、备份或通用解析通过。
- 新站必须按指定方向抓到缓存基准允许验证的期数，且至少形成有效历史数据后才能判重。
- 正式入库前必须完成专属解析、真实历史抓取、正式判重、目标期验证、成功/失败输出验证、站点级测试和重新抽抓验证。
- 新增验收默认只读；`onboard` 通过不等于已写入 `sites.json`，正式写入必须另行获得明确授权。
- 历史不足只能在用户明确批准的范围内作为特例处理；不得放宽方向、栏目、字段、来源、数量、边界、冲突或身份规则。

明确特例：

- `https://sgghdnx.47598995.app:17466/#xw` 是用户明确授权的9期历史不足特例，仅允许处理顶部的 `关公杀一肖`、`佛主禁肖图`、`三怪禁肖图` 三个目录，不能顺带抓取同页其他目录。
- `刘伯温咯` 可按用户明确授权作为9期历史不足特例，但仍须满足全部真实来源、方向、字段、专属解析和判重要求。
- `六合爆庄` 如已有配置明确归入已有站点分类，后续不得因新增流程移动分类。
- 特例只放宽明确授权的历史期数或目录范围，不放宽其他硬规则。

## 9. 缓存与正式判重

- 缓存文件为项目根目录 `recent_10_cache.json`，固定保存最多10条有效记录。
- 缓存记录按期数倒序排列，期数不得重复，生肖必须为合法单字符，`fingerprint` 必须与记录生肖顺序一致。
- 缓存身份至少绑定站名、URL、方向和分类；同URL下不同目录不得互相继承缓存或互相去重。
- 缓存记录应保留来源ID、来源URL和证据SHA-256；证据哈希必须是64位十六进制值。
- 新期写入后只保留不超过10条、且不晚于当前期的有效记录；禁止回滚到更早期数。
- 单期实时结果不得读取或依赖缓存来判期、判方向、解冲突或决定成功/失败；已有同期缓存值与新抓取值不同，只作为缓存更新提示，不能改写实时结果或写入失败TXT。
- 单期输出定稿后才允许滚动更新缓存；缓存读取、目录隔离、合并冲突或写入失败，只影响该目录的缓存更新，不得影响本轮任何站点的实时成功/失败判断。
- 新增站点必须命中正式 `recent_10_cache.json` 的缓存最新期或上一期；页面已经出现下一期时可以追加验证，但下一期不能替代缓存基准期。
- `recent_10_cache.json` 的业务读取只用于 `duplicate` 和 `onboard` 的新增站点重复检测；单期抓取不得把缓存当作候选来源或裁决来源。
- 判重只允许使用正式重复检测器、有效近10期缓存和候选站真实抓取结果。
- 无共同期号、抓取失败或数据为空时，判重未完成，不能报告为“不重复”；历史不足10期只能按上级明确特例处理。
- 判重按相同期号对齐，比较共同期号中的完整生肖序列；不得跨缺失期拼接。
- 判重命令固定使用 `--periods 10` 收集近10期窗口，但10期不是共同期号的判定门槛：连续1至2期一致不判重复，连续3至5期一致标记疑似重复，连续6期及以上判定重复；正式检测器不支持这些阈值时必须先修复，不能用“未满10期”绕过判定。

## 10. 输出与失败报告

- 成功文件：
  - `N期-肖.txt`：仅已有站点。
  - `N期-肖-新增.txt`：仅 `新增的站点`。
- 失败文件：
  - `N期-肖-失败.txt`：仅已有站点失败。
  - `N期-肖-失败-新增.txt`：仅新增站点失败。
- 成功和失败文件分别写入成功、失败目录，不得混放。
- 四类成功/失败TXT每轮都必须生成；对应分类没有内容时写入“无”，不得删除或跳过该TXT。
- 单个站点出现网络、解析、方向或候选问题时，只将该站点写入对应失败TXT，不得阻断其他站点输出。
- 缓存更新冲突或缓存损坏不得把实时成功站点写入失败TXT；最多跳过该目录的缓存更新，其他成功站点仍须正常写入成功TXT并更新缓存。
- 成功内容按生肖统计次数并生成生肖次数排行榜；输出顺序和项目现有格式不得擅自改变。
- 失败报告必须包含站点、URL、方向、指定期数、失败阶段和具体原因。
- 失败分类至少区分：网络、期数、方向、锚点、字段、候选冲突、边界、来源身份、专属解析未命中和其他失败；缓存更新提示不作为站点失败分类。
- 解析错误、期数错误、字段错误、候选冲突和边界错误不得伪装成网络失败。
- 正式TXT输出文件写入必须使用临时文件、刷新落盘和原子替换；TXT写入失败必须明确返回错误。缓存写入失败按缓存更新提示处理，不得改判实时结果。

## 11. 失败站修复

失败站修复必须依次经过：

`登记失败 -> 独立复现 -> 实验策略 -> 严格验证 -> 只读校验 -> 同步正式代码 -> 回归复审 -> 正式BAT验收`

- 登记时固定站点、URL、方向、目标期和原始失败原因；不得让命令行临时覆盖失败清单期数。
- 独立复现只运行失败清单中的目标站点，检查API、HTML、脚本、iframe、动态正文和必要浏览器渲染。
- 实验策略只能写入独立验证策略，必须同时包含专属解析器和同块证据检查器。
- 严格验证必须检查目标期、生肖唯一性、方向、标题/作者/栏目锚点、杀肖语义、候选顺序、重复记录、旧文档和跨文档干扰。
- 只读校验不得更新缓存，不得生成、删除或覆盖正式TXT；运行前后必须核对正式缓存和TXT的哈希。
- 只有独立验证完整通过后，才能同步最小专属解析改动；不得放宽通用解析、放宽方向边界或修改其他站点。
- 回归测试必须覆盖正确期、相邻期、错期、错栏目、错字段、块外候选、方向越界、同期冲突、重复候选和旧文档干扰。
- 最终必须执行正式单期BAT，以正式生成的分类成功/失败TXT和缓存作为验收依据。
- 任一环节缺失、仅通过模拟文本、单个正则或局部函数验证，均只能报告未完成或待验证。

## 12. 验收标准

- 配置可被加载，所有站点身份、方向、分类、解析器和取源策略合法。
- 指定期数的真实完整流程成功或明确失败，不能以旧缓存、相邻期、其他栏目或其他站点补位。
- 成功结果的期数、生肖、方向、来源、栏目、字段和原始顺序均可解释。
- 错期、方向越界、数量错误、非法生肖、跨文档借锚点和同期冲突均被拒绝。
- 单期正式流程先按实时结果写入对应TXT，再独立尝试更新对应期缓存；判重、新增验收、失败复抓和独立修复验证均不更新正式缓存或TXT。缓存更新失败不得回写或改判本轮TXT。
- 相关测试、项目完整测试、Python编译和JSON校验通过；真实验证不足或正式BAT未执行时，不得报告修复、新增或验收完成。
