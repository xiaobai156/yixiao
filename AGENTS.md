# 杀肖-V2 项目规则

本文件只适用于 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀肖-V2`，继承父目录 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`。父目录负责通用授权、来源隔离、数据准确性、缓存事务和安全边界；本文件只补充本项目差异。`AGENTS - 副本.md` 是只读历史副本，不得修改。

## 工作边界

- 用户当前指令优先；用户未说“开始”前只审计、解释或给方案，不抓取、测试、修改或写入。说“开始”后只处理已明确的项目、站点、URL、期数、方向、字段和文件。
- 解释、审计、诊断和只读验证不授权修复或正式写入；实施任务在范围明确后直接完成、适度验证并汇报，不重复索取已有授权。删除、封存、恢复、发布、消息发送、凭据处理和正式数据覆盖仍需明确授权。
- 范围只限本项目；不改其他子项目、不建跨项目公共库、不做跨项目统一架构。保留用户已有未提交改动，不顺带回退、清理或重建。
- 回复用中文，先称呼“老大”，结论优先、必要时编号；代码遵循 Ponytail full：先读实际调用链，优先复用，简单但不牺牲准确性。Skill 只在当前任务适用时使用，不把 Skill 的建议扩大为全局硬门禁。
- 新增、修复和失败复抓只运行用户指定站点；不得以测试、成功率或 BAT 验收为由运行全站。一个站点受阻时，继续处理同批相互独立的已授权站点。
- `origin` 已绑定 `https://github.com/xiaobai156/yixiao.git`。明确授权修改完成并通过必要验证后，只提交并推送本次改动的文件；不得带入已有未提交改动、凭据、临时文件或其他项目。只读任务不提交；无法安全区分时停止推送并报告。

## 入口与路径

- 源码：`src\zodiac_v2`；配置：`sites.json`；缓存：`recent_10_cache.json`；封存档案：`archived_sites.json`。
- BAT 先进入自身目录并设置 `PYTHONPATH=%CD%\src`；PowerShell：`$env:PYTHONPATH = "$PWD\src"`。
- 单期：`python -X utf8 -m zodiac_v2.cli single --period N --workers 8 --formal`。默认加载完整 `sites.json`，不用于定点验收。
- 判重：`python -X utf8 -m zodiac_v2.cli duplicate --period N --periods 10`，只读缓存，不是真实抓取。
- 失败复抓：`python -X utf8 -m zodiac_v2.cli retry --period N --retry-errors <失败文件>`；历史 `复抓失败.bat` 不存在，不得假报运行或自动重建。
- `onboard` 是新增站只读验收；`repair` 是失败站只读验证。默认成功目录为 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`，失败目录为 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`。

## 数据、来源与解析

- 成功结果必须是目标期的单个合法生肖（`牛马羊鸡狗猪鼠虎兔龙蛇猴`），并证明站点身份、URL/文章或主题 ID、栏目/作者锚点、字段语义、方向、数量、同一文档与区块、唯一性和原始顺序。期数为 `1..9999` 整数；不得猜期、跨期、跨站、跨文档或用缓存补值。
- `Candidate` 保留目标期、生肖、原始数据行、来源文档 ID、页面顺序和非空证据；配置必须有非空站名、HTTP/HTTPS URL、方向、分类、已注册 `parser_id` 和 `source_policy`。`pick` 与 `direction` 不共存；方向只接受 `top/顶部/上` 或 `bottom/尾部/底部/下`。
- 分类只有 `已有站点`、`新增的站点`；站名不得重复。来源身份至少绑定站名、规范化 URL、方向和分类，并按上级规则绑定栏目、字段和配置指纹。
- 同一高可信区块先收集全部有效候选并保留顺序，再执行首尾边界：`top` 取第一条、`bottom` 取最后一条；目标期不在对应边界、缺失、顺序不明、候选为空或同期结果冲突均失败。内部 `left` 不得用于正式配置。
- 每站使用已注册专属解析器，专属失败不得回落通用解析。期数、杀/禁/绝杀语义和生肖必须来自同一记录、表格行或明确数据块；API、HTML、script、iframe、OCR、渲染结果相互独立，不互借锚点或值。
- HTTP 已能完整、稳定、准确取到数据时固定 HTTP；确需 JS 才用配置允许的浏览器策略。浏览器只取动态正文，不裁决业务字段；尽量复用 Context。不能用复杂 fallback、代理池、熔断或调度掩盖网络、TLS、文章 ID、来源或冲突问题。动态文章优先 `api_url`；`api_then_http` 仅 API 404 时改页面。
- `special.user_forum_post` 只锁定配置用户自己的身份、主题、文章和正文；`http_consensus` 的 5 次取源、至少 2 次同值及下一期检查只适用于明确配置的站点。截图只作定位线索，必须再用真实页面/API 验证，不能替代来源证据或单独授权人工录入。

## 正式写入、缓存与 TXT

- 实时判定不读正式缓存补值；定稿后先写授权范围内的成功/失败 TXT，再独立持久化缓存。失败按真实阶段记录，缓存失败不得改判结果。
- `single --formal` 使用完整目标集合，缓存门禁为成功数/本轮目标站点数严格大于 85%；未通过时缓存原字节不变，但实时 TXT 仍生成。定点 `retry --formal` 使用失败清单的唯一匹配站点，调用 `commit_targeted_formal_single`，不走全量输出和 85% 门禁；成功只更新目标站，失败只更新目标期失败诊断并保留旧成功/缓存。
- 非正式 `single`、普通 `retry`、`repair`、`onboard`、判重、审计和影子验证不写正式配置、缓存或 TXT；验证产物写隔离目录。正式缓存最多 10 条有效记录，按期数倒序、无重复期数，保留来源 ID/URL、证据 SHA-256 和身份指纹。
- 已有站点写 `N期-肖.txt`、`N期-肖-失败.txt`；新增站点写 `N期-肖-新增.txt`、`N期-肖-失败-新增.txt`。定点合并复用 `merge_formal_single_result_output`：成功明细追加在排行榜前，保留其他行顺序；同站同值不重复，已有不同值只在用户明确授权后改写。失败只改目标站当前期，成功后清除对应失败记录，其他记录保持不变。
- 排行榜保持三列“内容、次数、排名”，次数降序，同次数并列且下一名连续递增；不自动追加“华林”。定点更新只改目标分类对应文件，不为补齐四类文件覆盖无关内容。

## 新增、修复、封存与验收

- 新站先做身份、真实来源、专属解析、方向窗口、历史和正式判重；默认至少 10 期有效历史。不足 10 期或连续 3–5 期重复，只有用户点名授权才可特例；连续 6 期及以上禁止加入。明确站点例外：`刘伯温咯` 可按授权用 9 期；已是 `已有站点` 的 `六合爆庄` 不因新增流程改类。
- `onboard` 通过只表示只读验收通过；获得正式添加授权后才写配置和正式单期结果。新增/修复不借本期任务建立历史、回填、改身份或迁移封存。
- 修复先“开始验证”隔离真实目标期，再由用户明确“开始正式更新”后迁移最小改动；封存站点不抓取、不写成功、不更新缓存，恢复/删除另需明确授权。
- 局部解析器/输出修改运行受影响测试和编译/静态检查；共享流程、统一校验、缓存、存储或入口修改再跑项目测试。真实站点修复必须跑指定站点完整单期链路；模拟结果不能冒充真实验收。纯文档修改只检查规则继承、内部引用、命令和 diff，不抓站、不读业务缓存、不跑全套测试。
- 报告区分已验证、已写入、失败和未完成；缺少真实验证或正式入口验收只能报部分完成。

## 例外记录

2026-09-09 的正式复抓约束：普通 `retry`/`repair` 只读；仅显式 `retry --formal` 可对失败清单中的指定期定点写入。多进程正式写入须从读取旧文件到持久化持有 `storage.locked_paths`；发现未完成 `.zodiac-txn-*.json` 事务时拒绝覆盖并按指定标记恢复。历史 238/240/241 期快照测试仅在 `--historical-snapshots` 下运行，不为通过历史断言回滚当前数据。
